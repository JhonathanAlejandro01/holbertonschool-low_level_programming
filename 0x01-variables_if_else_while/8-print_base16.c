
#include <stdio.h>

/**
 * main - base 16
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char a;

	char b;

	for (a = 48; a < 58; a++)
	{
		putchar(a);
	}
	for (b = 97; b < 103; b++)
	{
		putchar(b);
	}
	putchar(10);
	return (0);
}
